<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <div class="container">
    <h2>{{ message }}</h2>
    <h1>{{ title }}</h1>
    <form action="/auth/github" method="GET">
      <div class="submit">
        <button class="fancy-button" type="submit">Authorize Github - create repo</button>
      </div>
    </form>
  </div>
</body>
</html>
<style>
    body {
      background-color: #0d0c1d;
      color: #98c7f7;
      font-family: Arial, sans-serif;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      text-align: center;
    }

    h1 {
      font-size: 48px;
      margin-bottom: 30px;
      font-weight: bold;
      font-family: 'Times New Roman', serif;
      color: #dfeaff;
    }

    h2 {
      font-size: 24px;
      margin-bottom: 20px;
      font-style: italic;
      color: #dfeaff;
    }

    .fancy-button {
      background-color: #1e205d;
      color: #98c7f7;
      font-size: 24px;
      padding: 16px 32px;
      border: none;
      border-radius: 50px;
      cursor: pointer;
      box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s ease;
    }

    .fancy-button:hover {
      background-color: #252886;
    }
  </style>